# Core dependencies
httpx>=0.27.0              # HTTP client (no requests)
beautifulsoup4>=4.12.0     # HTML parsing for ThreadReaderApp fallback
pydantic>=2.0.0            # Data validation and models
aiohttp>=3.9.0             # Async HTTP server for webhook

# LLM integration
anthropic>=0.40.0          # Claude API
google-genai>=1.0.0        # Gemini API (for YouTube processing)
tiktoken>=0.7.0            # Token estimation for Stage 2 budget

# Development
pytest>=8.0.0              # Testing
pytest-asyncio>=0.24.0     # Async test support
ruff>=0.8.0                # Linting
